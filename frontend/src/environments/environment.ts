// This file is required by Angular CLI resolve the workspace root path.
// Alternatively, you can `import` or `require` your workspace `package.json`
// as shown below:
//
// import * as packageJson from '../package.json';
// const version = packageJson.version;
//
// It's also possible to use the `require.main.filename` as the root directory:
// const rootPath = require('main').filename
//   .split(require('path').sep)
//   .slice(0, -2)
//   .join(require('path').sep);

export const environment = {
  apiUrl: 'http://localhost:3000',
  appName: 'Blog CMS',
};
